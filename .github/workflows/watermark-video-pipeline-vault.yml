name: Watermark Video Pipeline - With Vault

permissions:
  contents: read
  actions: read
  id-token: write

concurrency:
  group: watermark-video-pipeline
  cancel-in-progress: false

on:
  workflow_dispatch:
    inputs:
      class_date_list:
        description: 'Comma-separated list of class dates (YYYYMMDD, leave blank for today)'
        required: false
      s3_delete_original:
        type: boolean
        default: false
      delete_attendance:
        type: boolean
        default: false
      delete_class:
        type: boolean
        default: false
      s3_delete_watermark:
        type: boolean
        default: false
      use_s3_original:
        type: boolean
        default: false
      vemio_delete:
        type: boolean
        default: false

  schedule:
    - cron: "30,10 5-9 * * 1-6"
    - cron: "30,10 14-16 * * 1-6"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  prepare-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}

    steps:
      - name: Checkout private source repo
        uses: actions/checkout@v4
        with:
          repository: devopscalendar101/wv_source
          token: ${{ secrets.PAT }}
          path: wv_source

      - name: Generate matrix from batch config
        id: set-matrix
        run: |
          set +x
          CONFIG=$(cat wv_source/conf/config.json)
          BATCHES=$(echo "$CONFIG" | jq -c '[.batches[] | select(.enabled == true)]')

          if [ -n "${{ github.event.inputs.class_date_list }}" ]; then
            CLASS_DATES="${{ github.event.inputs.class_date_list }}"
          else
            CLASS_DATES=$(date -u +%Y%m%d)
          fi

          MATRIX=$(jq -n -c \
            --argjson batches "$BATCHES" \
            --arg dates "$CLASS_DATES" \
            '{
              include: [
                $batches[] as $batch |
                ($dates | split(",") | .[] | gsub("^\\s+|\\s+$";"")) as $date |
                {
                  batch_name: $batch.batch_name,
                  course_id: $batch.course_id,
                  itd_zoom_id: $batch.itd_zoom_id,
                  batch_id: $batch.batch_id,
                  zoom_id: $batch.zoom_id,
                  zoom_account: $batch.zoom_account,
                  parent_topic: $batch.parent_topic,
                  class_date: $date
                }
              ]
            }')

          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT

  process-videos:
    needs: prepare-matrix
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare-matrix.outputs.matrix) }}

    name: "${{ matrix.batch_name }}_${{ matrix.class_date }}"

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Checkout private source repo
      uses: actions/checkout@v4
      with:
        repository: devopscalendar101/wv_source
        token: ${{ secrets.PAT }}
        path: wv_source

    - name: Cache pip packages
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: pip-${{ hashFiles('wv_source/conf/requirements.txt') }}

    - name: Install Vault CLI and PostgreSQL
      run: |
        set +x
        wget -qO- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
        echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list > /dev/null
        sudo apt-get update -qq && sudo apt-get install -y -qq vault postgresql-client

    - name: Setup Environment
      run: |
        set +x
        chmod +x wv_source/bin/bootstrap.sh
        wv_source/bin/bootstrap.sh install
        python3 -m pip install --upgrade pip -q
        python3 -m pip install -r wv_source/conf/requirements.txt -q
        chmod +x wv_source/bin/*

    - name: Retrieve Vault Credentials (in-memory, masked)
      env:
        VAULT_ADDR: ${{ secrets.VAULT_URL }}
        SERVICE_TOKEN: ${{ secrets.SERVICE_TOKEN }}
      run: |
        set +x
        set -euo pipefail

        CREDS_FILE="$RUNNER_TEMP/.creds.env"
        umask 077
        : > "$CREDS_FILE"

        # Helper: register secret with GitHub (so it is redacted everywhere) and write to creds file
        mask_and_save() {
          local name="$1" value="$2"
          if [ -z "$value" ]; then
            echo "::error::$name is empty"
            return 1
          fi
          echo "::add-mask::$value"
          # Use printf %q so values with special chars survive shell sourcing
          printf 'export %s=%q\n' "$name" "$value" >> "$CREDS_FILE"
        }

        export VAULT_TOKEN="$SERVICE_TOKEN"

        ALL_TOKEN=$(vault kv get -address="$VAULT_ADDR" -field=toc7kawoc7 -mount=hej0SESwEs cicuwrl4po 2>/dev/null) || { echo "::error::Failed to retrieve ALL_TOKEN"; exit 1; }
        echo "::add-mask::$ALL_TOKEN"
        export VAULT_TOKEN="$ALL_TOKEN"

        ITD_TEST_TOKEN=$(vault kv get -address="$VAULT_ADDR" -field=wr0swu5udi -mount=driy9s4uce je0emad4ov 2>/dev/null) || { echo "::error::Failed to get ITD_TEST_TOKEN"; exit 1; }
        ZOOM_TOKEN=$(vault kv get -address="$VAULT_ADDR" -field=v0ve2regos -mount=driy9s4uce je0emad4ov 2>/dev/null) || { echo "::error::Failed to get ZOOM_TOKEN"; exit 1; }
        AWS_ROOT_TOKEN=$(vault kv get -address="$VAULT_ADDR" -field=vepiz9pafr -mount=driy9s4uce je0emad4ov 2>/dev/null) || { echo "::error::Failed to get AWS_ROOT_TOKEN"; exit 1; }
        echo "::add-mask::$ITD_TEST_TOKEN"
        echo "::add-mask::$ZOOM_TOKEN"
        echo "::add-mask::$AWS_ROOT_TOKEN"

        # DB credentials
        export VAULT_TOKEN="$ITD_TEST_TOKEN"
        mask_and_save PGHOST     "$(vault kv get -address="$VAULT_ADDR" -field=DB_HOST     -mount=ql1te2icha bruxe0az6q 2>/dev/null)"
        mask_and_save PGPORT     "$(vault kv get -address="$VAULT_ADDR" -field=DB_PORT     -mount=ql1te2icha bruxe0az6q 2>/dev/null)"
        mask_and_save PGDATABASE "$(vault kv get -address="$VAULT_ADDR" -field=DB_NAME     -mount=ql1te2icha bruxe0az6q 2>/dev/null)"
        mask_and_save PGUSER     "$(vault kv get -address="$VAULT_ADDR" -field=DB_USER     -mount=ql1te2icha bruxe0az6q 2>/dev/null)"
        mask_and_save PGPASSWORD "$(vault kv get -address="$VAULT_ADDR" -field=DB_PASSWORD -mount=ql1te2icha bruxe0az6q 2>/dev/null)"

        # Vimeo credentials
        mask_and_save VIMEO_ACCESS_TOKEN  "$(vault kv get -address="$VAULT_ADDR" -field=VIMEO_ACCESS_TOKEN  -mount=ql1te2icha bruxe0az6q 2>/dev/null)"
        mask_and_save VIMEO_CLIENT_ID     "$(vault kv get -address="$VAULT_ADDR" -field=VIMEO_CLIENT_ID     -mount=ql1te2icha bruxe0az6q 2>/dev/null)"
        mask_and_save VIMEO_CLIENT_SECRET "$(vault kv get -address="$VAULT_ADDR" -field=VIMEO_CLIENT_SECRET -mount=ql1te2icha bruxe0az6q 2>/dev/null)"

        # AWS credentials
        export VAULT_TOKEN="$AWS_ROOT_TOKEN"
        mask_and_save AWS_ACCESS_KEY_ID     "$(vault kv get -address="$VAULT_ADDR" -field=thujostav9 -mount=yi6pobrath s7ohebu2ho 2>/dev/null)"
        mask_and_save AWS_SECRET_ACCESS_KEY "$(vault kv get -address="$VAULT_ADDR" -field=yuru9ibruw -mount=yi6pobrath s7ohebu2ho 2>/dev/null)"
        mask_and_save AWS_DEFAULT_REGION    "ap-south-1"

        # Zoom credentials
        export VAULT_TOKEN="$ZOOM_TOKEN"
        mask_and_save Z1_AI "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_ACCOUNT_ID_ORG1   -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z1_CI "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_CLIENT_ID_ORG1    -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z1_CS "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_CLIENT_SECRET_ORG1 -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z2_AI "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_ACCOUNT_ID_ORG2   -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z2_CI "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_CLIENT_ID_ORG2    -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z2_CS "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_CLIENT_SECRET_ORG2 -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z4_AI "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_ACCOUNT_ID_ORG4   -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z4_CI "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_CLIENT_ID_ORG4    -mount=t1lrophebr sestip6ecu 2>/dev/null)"
        mask_and_save Z4_CS "$(vault kv get -address="$VAULT_ADDR" -field=ZOOM_CLIENT_SECRET_ORG4 -mount=t1lrophebr sestip6ecu 2>/dev/null)"

        # ITD token from GitHub secrets (already masked, but register from this context too)
        ITD_TOKEN_VALUE='${{ secrets.ITD_TOKEN }}'
        if [ -n "$ITD_TOKEN_VALUE" ]; then
          echo "::add-mask::$ITD_TOKEN_VALUE"
          printf 'export ITD_TOKEN=%q\n' "$ITD_TOKEN_VALUE" >> "$CREDS_FILE"
        fi

        chmod 600 "$CREDS_FILE"

        # Verify DB connectivity without echoing values
        # shellcheck disable=SC1090
        source "$CREDS_FILE"
        if ! PGPASSWORD="$PGPASSWORD" psql -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE" -c "SELECT 1;" >/dev/null 2>&1; then
          echo "::error::Database connection failed"
          exit 1
        fi

        unset VAULT_TOKEN SERVICE_TOKEN ALL_TOKEN ITD_TEST_TOKEN ZOOM_TOKEN AWS_ROOT_TOKEN
        echo "Credentials retrieved and validated"

    - name: Download watermark assets from S3
      run: |
        set +x
        # shellcheck disable=SC1091
        source "$RUNNER_TEMP/.creds.env"

        WM_DIR="$HOME/.watermark-assets"
        mkdir -p "$WM_DIR"
        aws s3 cp s3://class-recordings-itdefined/watermark-assets/ "$WM_DIR/" --recursive --quiet
        echo "Assets downloaded:"
        ls -lh "$WM_DIR/"
        # Non-secret env exports only
        echo "WM_PATH=$WM_DIR" >> $GITHUB_ENV
        echo "PATH=$GITHUB_WORKSPACE/wv_source/bin:$PATH" >> $GITHUB_ENV

    - name: Set Zoom Email from Account
      id: zoom-email
      run: |
        set +x
        case "${{ matrix.zoom_account }}" in
          Z1) ZOOM_EMAIL="itdefined.org@gmail.com" ;;
          Z2) ZOOM_EMAIL="itdefined.org2@gmail.com" ;;
          Z3) ZOOM_EMAIL="itdefined.org3@gmail.com" ;;
          Z4) ZOOM_EMAIL="itdefined.org4@gmail.com" ;;
          *)  ZOOM_EMAIL="unknown@gmail.com" ;;
        esac
        echo "zoom_email=$ZOOM_EMAIL" >> $GITHUB_OUTPUT

    - name: Generate topic from date
      id: topic
      run: |
        set +x
        DATE="${{ matrix.class_date }}"
        YEAR="${DATE:0:4}"
        MONTH="${DATE:4:2}"
        DAY="${DATE:6:2}"
        TOPIC="Topic ${YEAR}-${MONTH}-${DAY}"
        echo "topic=$TOPIC" >> $GITHUB_OUTPUT

    - name: Pipeline Options Check
      run: |
        set +x
        S3_DEL_ORIG="${{ github.event.inputs.s3_delete_original || 'false' }}"
        S3_DEL_WM="${{ github.event.inputs.s3_delete_watermark || 'false' }}"
        VEMIO_DEL="${{ github.event.inputs.vemio_delete || 'false' }}"
        USE_S3="${{ github.event.inputs.use_s3_original || 'false' }}"
        DEL_ATT="${{ github.event.inputs.delete_attendance || 'false' }}"

        icon() { [[ "$1" == "true" ]] && echo "ON" || echo "OFF"; }

        echo "Pipeline options:"
        printf "  use_s3_original     : %s\n" "$(icon $USE_S3)"
        printf "  s3_delete_original  : %s\n" "$(icon $S3_DEL_ORIG)"
        printf "  s3_delete_watermark : %s\n" "$(icon $S3_DEL_WM)"
        printf "  vemio_delete        : %s\n" "$(icon $VEMIO_DEL)"
        printf "  delete_attendance   : %s\n" "$(icon $DEL_ATT)"

        ACTIVE=0
        [[ "$S3_DEL_ORIG" == "true" ]] && ACTIVE=$((ACTIVE+1))
        [[ "$S3_DEL_WM"   == "true" ]] && ACTIVE=$((ACTIVE+1))
        [[ "$VEMIO_DEL"   == "true" ]] && ACTIVE=$((ACTIVE+1))
        [[ "$DEL_ATT"     == "true" ]] && ACTIVE=$((ACTIVE+1))
        if [[ $ACTIVE -ge 3 ]]; then
          echo "::warning::$ACTIVE delete flags active — this will re-process most of the pipeline"
        fi
        if [[ "$S3_DEL_ORIG" == "true" && "$USE_S3" == "true" ]]; then
          echo "::warning::s3_delete_original + use_s3_original both ON — will delete then try to download from same bucket"
        fi

    - name: Run Watermark Video Pipeline
      id: pipeline
      run: |
        set +x
        # shellcheck disable=SC1091
        source "$RUNNER_TEMP/.creds.env"
        set +e
        wv_source/bin/watermark_pipeline.sh \
          "class-recordings-itdefined" \
          "${{ matrix.batch_name }}" \
          "${{ matrix.itd_zoom_id }}" \
          "${{ matrix.zoom_account }}" \
          "${{ steps.topic.outputs.topic }}" \
          "${{ matrix.course_id }}" \
          "${{ matrix.batch_id }}" \
          "${{ matrix.parent_topic }}" \
          "${{ matrix.zoom_id }}" \
          "${{ steps.zoom-email.outputs.zoom_email }}" \
          "${{ matrix.class_date }}" \
          "${{ github.event.inputs.s3_delete_original || 'false' }}" \
          "${{ github.event.inputs.delete_attendance || 'false' }}" \
          "${{ github.event.inputs.delete_class || 'false' }}" \
          "${{ github.event.inputs.s3_delete_watermark || 'false' }}" \
          "${{ github.event.inputs.use_s3_original || 'false' }}" \
          "$VIMEO_ACCESS_TOKEN" \
          "$VIMEO_CLIENT_ID" \
          "$VIMEO_CLIENT_SECRET" \
          "${{ github.event.inputs.vemio_delete || 'false' }}"
        PIPELINE_EXIT=$?
        if [ $PIPELINE_EXIT -eq 2 ]; then
          echo "::warning::No video available for ${{ matrix.batch_name }} on ${{ matrix.class_date }} (no class or Zoom still processing)"
          echo "status=warning" >> $GITHUB_OUTPUT
        elif [ $PIPELINE_EXIT -ne 0 ]; then
          echo "::error::Pipeline failed for ${{ matrix.batch_name }} on ${{ matrix.class_date }}"
          echo "status=failed" >> $GITHUB_OUTPUT
          exit 1
        else
          echo "status=success" >> $GITHUB_OUTPUT
        fi

    - name: Upload artifacts (if failed)
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: logs-${{ matrix.batch_name }}-${{ matrix.class_date }}
        path: |
          ~/.tmp/${{ matrix.batch_name }}/*.log
          ~/.tmp/${{ matrix.batch_name }}/*.txt
        retention-days: 7
        if-no-files-found: ignore

    - name: Cleanup local videos
      if: always()
      run: rm -rf ~/.tmp/${{ matrix.batch_name }}/*.mp4 || true

    - name: Cleanup credentials
      if: always()
      run: |
        set +x
        CREDS_FILE="$RUNNER_TEMP/.creds.env"
        if [ -f "$CREDS_FILE" ]; then
          shred -vfz -n 3 "$CREDS_FILE" 2>/dev/null || rm -f "$CREDS_FILE" || true
        fi
