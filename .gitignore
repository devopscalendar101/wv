# Prevent committing unencrypted scripts (only .enc versions should be committed)
extras/s3_tool
extras/vemio_upload
extras/watermark
extras/zoomd
extras/zoom_attendance
extras/s3_tool_simple.py

# But DO commit the encrypted versions
!extras/*.enc

# Commit these public files
!extras/watermark_pipeline.sh
!batch-config.json
!requirements.txt
!*.yml
!*.sh

# Local development
.venv/
__pycache__/
*.pyc
.DS_Store

# Sensitive local config files (DO NOT COMMIT)
retrieve_credentials.sh
run_pipeline_locally.sh
test_pipeline_locally.sh
build.sh
.env.local
encrypt.sh
decrypt.sh

# Source code for local development (unencrypted)
bin/scripts/

# Local overrides (optional - commit template instead)
# pipeline_config.json

# Test/temp files
.tmp/
*.log
.pytest_cache/

# Documentation (optional - ignore or commit as needed)
*.md
